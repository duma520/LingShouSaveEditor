# LingShouSaveEditor - 软件升级迭代记录

> 灵兽江湖存档修改器 · Python 3.10 + PySide6 (Qt6)
> 当前版本：**v2.6.0**（2026-09-05）
> 主程序：`LingShouSaveEditor.py`（唯一编译源，最终更新版）
> 作用：读取/粘贴 灵兽江湖 `*.es3` 存档，枚举并修改 **"ItemHad"** 段道具名称与数量，一键 9999、单个编辑、新增/删除/**一键添加全部**道具、**一键 _GetTimeNew→0**，其余数据段逐字节保留；每次读档自动把 ItemHad 道具名去重收录到 **道具名.json** 道具名库，并可**一键整理道具名**把当前存档(表格)已有道具名即时补入 json；**「打开上次」+「最近 ▾」历史（历史记录.json）**一键重开上次/历史文件，**「重新读取」**在游戏再次存档后重读当前文件；点表头可按**名称/数量排序**（不影响修改，支持升/降双向：点击=升序、同列再点=降序、右键可直接选方向）；**新增道具**可选/搜（中文·拼音全拼·首字母·数字）、多选/全选一次添加多个，并可勾选**仅显示缺少**与个人背包对比后一键补齐；**批量目标值持久保存**（设置.json）——「全部改为」目标值改后自动记住、重开仍保留，左侧按钮同步显示；**多角色分页(v2.0.0)**——主区按「道具 | 主角 | 每个队友」分页签，可对主角与**全部队友**逐字段改等级/经验/属性/血量/特性点/已用点等（带一键默认），整组改 武学经验(KongFuTypeLv) / 生活技能(LifeExp) / 六维(SixProCurrent) / 好感度(FriendLoveNum)，并可新增/删除地图标注点(_ActiveMap)；**队友按 _AbName 自动枚举(v2.1.0)**——不论在队(AddFriends)、离队(LeaveFriends)还是以后游戏新增的容器/队友都会自动出现可改页签；**大档后台线程打开(v2.2.0)**——超过约 600KB 的存档解析/角色模型改在子线程完成，打开/重新读取窗口不卡死，另把角色页改为**数据驱动建模**（`char_model` 单遍收集）大幅降低建页耗时。**道具搜索过滤(v2.3.0)**——道具页表格上方新增搜索框（与「新增道具」一致的中文/拼音全拼/首字母/数字 + 匹配方式切换），输入即实时过滤当前表格，便于快速定位要删除/修改的道具（# 列仍显示完整列表原序号）；双击编辑、删除选中只作用于过滤后的可见行，清空搜索框即恢复全部；新增/一键添加道具后自动清过滤以显示新行。**地图标注点搜索防重(v2.3.1)**——地图标注点(_ActiveMap)输入框输入时实时过滤下方已有地点便于查重，添加与已有相同的地点会被拦截并提示，不会重复。**地图跳转点库/批量添加(v2.4.0)**——`_scaffold/build_map_jump_points.py` 从 MapJumpTable.bytes(ES3 二进制, List<MapJumpTableItem>)提取每条记录 name(跳转点显示名, 含无 id 记录)去重排序生成程序目录 `地图跳转点.json`(236 个, 真实档 _ActiveMap 29 全覆盖); 地图标注点区新增「从地图跳转点批量添加…」——搜索(中文/拼音)/多选/全选一次添加多个, 默认只列未标注的, 与已有重复自动跳过。**新增道具按分类浏览(v2.6.0)**——「新增道具」弹窗改左右双栏：左栏分类树(勾选)按 内存所有道具.json 的 dataset 路径分级(大分类→小分类→细分类)，覆盖 equip装备/item道具/recipe配方/bookcontent书籍/kangfu武功秘籍/skillcom技能合成/shanhailu山海录 7 大类；勾选某分类即把该分类及全部下级道具纳入右侧候选(可多选大/小/细分类、勾「大分类」自动含所有下级、展开反选子分类可精确排除[父自动半选])；右栏保留搜索/多选/全选/自定义名/「仅显示缺少」，分类+搜索可叠加。数据源 程序目录 `道具分类.json`(由 `_scaffold/build_item_categories.py` 生成)，缺失自动回退 道具名.json 平铺。

---

## 一、迭代记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v2.6.0 | 2026-09-05 | **新增道具按分类浏览(左右双栏, 大→小→细分类)**：新增 `_scaffold/build_item_categories.py` 从 内存所有道具.json 的 dataset/<大分类>/<小分类>/…/<道具名> 生成 程序目录 `道具分类.json`({version, zh:7大分类中文, cats:[{code, groups:{相对路径(段间/连接):[道具名…]}}]}, 4977 名/覆盖 7 大分类: equip装备(435)/item道具(2480)/recipe配方(644)/bookcontent书籍(194)/kangfu武功秘籍(1044, 桶内重名已去)/skillcom技能合成(155)/shanhailu山海录(25), 与 道具名.json 来源一致)。主程序加 `ITEM_CAT_ZH`(1109)/`get_item_categories_path`(1115)/`load_item_categories`(1120, 缺失返回 None 触发回退)/`build_item_cat_nodes`(1164, 纯函数→(roots,all_names) 跨分类/路径去重, 节点含 own/children/total)。「新增道具」`AddItemDialog`(1340) 改<b>左右双栏</b>: 左 `_build_category_pane`(1449) 建 QTreeWidget 分类树(勾选框, 大分类根显示 `code·中文(总数)`, 展开到 0 级; 按钮 展开全部/折叠/全勾选/取消勾选); 勾选语义 = 含该分类全部下级(勾父向下传播 `_set_subtree_state` 1509、反选子分类父自动半选 `_refresh_cat_ancestors` 1515、`_on_cat_changed` 1532→`_selected_names` 1562 DFS: Checked=整棵/Unchecked=跳过/半选=自身直挂名+各子级按状态), 候选范围 `_current_scope`(1594, **默认不勾选**: 勾某分类/子分类即窄化右侧、空态给引导、未勾选但有搜索词=全库给建议); 右栏沿用 搜索/多选/全选/自定义名/「仅显示缺少」(`_filtered` 1609 按 scope), 分类+搜索叠加; 「全勾选」=全部道具。缺 道具分类.json 自动回退 道具名.json 平铺(无左树)。GUI 冒烟新增分类树用例(小数据集打桩 load_item_categories), 154/154 通过; 核心 108/108(含带引号名转义往返、商店 Discount/RefreshDay 参数、SellEquips 装备纯函数、分类/拼音缓存命中用例)。另: 带引号特殊名(碎“肉”、剑令“地绝”等 6 个)——es3 写文件时名字含中文弯引号 “ ” 自动转义为 反斜杠+引号(与游戏存档存储一致), 读回自动还原; 道具名.json/界面均存“干净名”(旧收录的 碎\“肉\” 已规整为 碎“肉”)。另: 道具数量上限由 999,999,999 提到 **2,100,000,000(21亿)**——统一常量 `MAX_ITEM_COUNT`(新增道具/全部改为 数字框、单元格校验、设置恢复 clamp 均引用它)。再另: 「商店NPC」页新增修改 NPC 参数——折扣 **Discount**(1=无折扣/0.5=五折/范围 0~1, 默认一键 0.1)、刷新日 **RefreshDay**(默认一键 1)——纯函数 `shop_field`/`shop_set_field` + 右侧「NPC 参数」行(选中即载入, 改动即写入; 无该字段的店禁用)。还另: 「商店NPC」页新增往 **SellEquips** 装备栏添加**装备类道具**(道具分类.json 的 equip 类, 名含前导数字=等级, 如 1铁盔→_Lv 1)——新增纯函数 `_shop_sellequips_open`/`shop_equip_names`(读已上架装备名+件数, 返回干净名)/`shop_add_equips`((名,件数)列表→同名已有向该名数组追加副本、新名新增条目、空 SellEquips 在 `{` 后内联首键; 模板字段 `_Name`/`_Lv`(前导数字)/`_Durable`100/`_SkillLv`/`_ItemSkillLv` 空对象/`_DressType`1/`_GetTimeNew`0/`_IsCanLingHua`true/`_IsFirstGet`0/`_LockDissolve`false/_QiItemNames 空数组, 缩进按所在档 L0+unit 推导, 兼容制表符+CRLF), 返回 (新文本, 新增名→件数)); GUI 按钮「＋ 添加装备…(SellEquips)」(`_on_shop_add_equip`, 复用 AddItemDialog 多选 equip, 件数=副本数, 同名不灰显可再追加), shop_title 显示 装备 N 种/M 件; 文本改写后 `_resync_shop_cache()` 重算所有商店 SellItems 容器开括号缓存(否则后续 添加卖品 用旧偏移会串位)。 再另: **多线程全覆盖 + 表格局部刷新(避免大档卡顿, v2.6.0 内)**——①统一后台助手 `_run_bg`(3364, 后台线程 + done 标志 + 忙光标 + 主线程泵事件), `_open_big`(3395) 重构为调它; 大段粘贴 `_paste_big`(3519, ≥600KB 后台解析/角色模型/收录)、保存 `_on_save`(3555)/另存 `_on_save_as`(3577, 重建+写盘后台)、复制 `_on_copy`(3604, 重建后台, 剪贴板回主线程)——大档这些入口全程窗口可响应不卡; ②表格局部化: `_reload_table`(2363) 重建期间 setUpdatesEnabled(False)+blockSignals(几千行约 -20% 且防逐行闪烁), 批量改 `_on_set_all_value`(3648) 改走 `_sync_visible_counts`(2393) 只刷数量列(4561 行整表重建 ~2.2s → ~0.13s), 删除 `_on_delete`(3718) 无过滤时走 `_delete_rows_local`(3744) removeRow 局部删 + 仅顺延 # 号(~2.2s → ~0.25s), 过滤下仍整表重建(行少最稳); ③新增道具对话框提速: `load_item_categories`(1317)/`load_pinyin_map`(395) 结果按 path 内存缓存(`_CAT_CACHE` 1314 / `_PINYIN_CACHE` 392), 多次打开不再重复读/解析 json。GUI 新增 局部批量改/删除#顺延/后台保存/复制/大文本粘贴 用例(154/154); 核心新增 缓存命中 用例(108/108)。 |
| v2.5.1 | 2026-09-05 | **商店NPC 分类筛选(地方/门派)**：新增 `_scaffold/build_shop_cities.py` 从 内存所有道具.json 的 分类/shop(路径 dataset/shop/<分类>/…/<店名>) 解析店名→分类代码, 生成 程序目录 `商店分类.json`(14 个分类/307 条映射: anju安居城/hexing禾兴城/guanglin广临城/heishi黑市/linzhou临州城/longju龙居城/menpai门派(97)/other其他/wutian吾天城/xianghuocun乡火村/xining息宁城/yuehan岳汉城/chapter5第五章秘境/tilemap23shop等)。主程序加 `SHOP_CITY_ZH`(1062)/`get_shop_cities_path`(1071)/`load_shop_cities`(1076)。「商店NPC」页签左侧顶部新增<b>分类下拉</b>(全部+各分类[中文(代码)]+若有未分类店加「(未分类)」), 选中即按地方/门派过滤 NPC(`_refresh_shop_tab` 1917 读库并给每个 NPC 项存分类代码; `_filter_shop_list` 1966 分类+中文/拼音/首字搜索叠加 setHidden 过滤), 无法匹配 商店分类.json 的店(如玩家改名店)归未分类。GUI 冒烟新增分类用例(打桩 load_shop_cities), 126/126 通过; 核心 84/84。 |
| v2.5.0 | 2026-09-05 | **NPC商店卖的道具(SellItems) 查看/添加**：①行级引擎参数化 `find_itemhad_line`(62)/`parse_itemhad`(72)/`apply_itemhad_text`(202) 增 `key` 参数(默认 ItemHad)——商店 SellItems 的道具字典段与 ItemHad 结构完全一致(5字段), 传 key="SellItems" 即可解析/重建; 段尾兼容 `},`/`}`(该段若为所在对象末尾字段则无逗号)。②新增 `SCOPE_SHOP`(962)=saveUtilData.value._ShopData 与纯函数 `shop_records`(966, 一次枚举全部商店 (键,abName,SellItems开括号)——避免逐店全量大扫)、`shop_sellitems`(1001)、`shop_add_items`(1023, 与已有/本次同名自动跳过, 指定数量, 整段重建)。③GUI 新增「商店NPC」页签(固定 index=1, 角色页相应移到 index2+): 左侧 NPC/商店列表支持 中文/拼音/首字 搜索(`_filter_shop_list` 1897, setHidden 过滤), 选中即显示其 SellItems 卖品表(`_show_shop_items` 1924); 「添加卖品…」(`_on_shop_add` 1943) 复用 AddItemDialog(道具名.json 搜索/多选, 数量默认 999), 与已有同名自动跳过, 成功写回 self._text 随「保存/另存为/复制结果」落盘。真实档(custom0.es3, _ShopData 322 家)内存验证: 加 2 件+重名跳过/空店可加/段尾无逗号可加/括号平衡。核心 84/84、GUI 121/121 通过。 |
| v2.4.1 | 2026-09-04 | **程序图标(icon.ico)**：新增 `app_icon()`(259, 读 程序目录/icon.ico, 缺失返回空 QIcon 回退系统默认); `main()`(2762) 经 `QApplication.setWindowIcon` 设应用图标、`MainWindow.__init__` 显式 `self.setWindowIcon(app_icon())`, 主窗口/对话框/任务栏统一显示 icon.ico。打包: `0_nuitka_pyside6_clang_auto.bat` 增加 `--windows-icon-from-ico=icon.ico`(exe 图标) + `--include-data-files=icon.ico=icon.ico`(运行时读, 放 exe 旁)。GUI 冒烟新增 2 条图标断言(图标来自 icon.ico/主窗口已设), 114/114 通过; 核心 77/77。 |
| v2.4.0 | 2026-09-04 | **地图跳转点提取(地图跳转点.json) + 批量添加地图标注点**：①新增脚手架 `_scaffold/build_map_jump_points.py` 解析 MapJumpTable.bytes(ES3 二进制, 存 List<MapJumpTableItem> 字段 id/name/name2/x/y/icontype/beizhu)——name 是跳转点显示名(与存档 _ActiveMap 同体系, 如 禾兴城/临州城/泾村竹林/良人楼)。脚本把字节流切成 ascii(id)与中文 token: 每个「id 后紧随首个中文」=name(主来源), 无 id 记录(良人楼/金光洞/龙居城大道等)其 name 会漏成孤立中文——将其中非动作说明类(剔除 右/左/传送/从xx/坐xx/第X章/其实用不到 等)并入; 去重排序写程序目录 `地图跳转点.json`(顶层纯数组, 236 个, 真实档 _ActiveMap 29 全覆盖)。②新增库函数 `get_map_jump_path`(325)/`load_map_jump_names`(330)。③地图标注点区新增按钮「从地图跳转点批量添加…」→ `_on_add_maps`(2057) 打开新对话框 `MapJumpDialog`(1230, 仿新增道具): 列表=跳转点.json 全部候选, 支持 中文/拼音 搜索 + 匹配方式、Ctrl/Shift 多选/全选、双击单项; 默认勾选「仅显示未标注(不在 _ActiveMap)」便于一次补齐; 已在 _ActiveMap 的灰显不可选(取消勾选可看); 确定后对每个名字调 `add_str_array`(与已有重复自动跳过)并刷新列表。GUI 冒烟测试新增对话框+打桩批量主流程用例(全选添加且重复自动跳过/空库不崩溃), 112/112 通过; 核心 77/77 通过。 |
| v2.3.1 | 2026-09-04 | **地图标注点输入即搜索 + 同名拦截防重**：地图标注点(_ActiveMap)的输入框（`_map_edit`）接 `textChanged` → 新方法 `_map_filter`(1853)——输入时把下方 `_map_list` 中不包含关键字的行 `setHidden(True)`（仅隐藏显示、不影响数据与删除），清空输入即恢复全部，方便查看该地点是否已存在；`_map_add`(1871) 在调用 `add_str_array` 前先用 `get_str_array` 检查，**输入与已有地点同名时不再添加**——状态栏提示「地点已存在: X(未重复添加)」、清空输入并定位/选中已存在的那一项；成功添加仍走原逻辑（`add_str_array` 重名跳过为纯函数兜底）；`_map_refresh`(1865) 末尾重应用当前过滤词。GUI 冒烟测试新增 v2.3.1 段（初始全可见/输入'竹'过滤只显泾村竹林/完整输入过滤/添加同名被拦截列表不变且不脏/新地点正常添加/过滤下删除可见项/清空恢复全部可见），103/103 通过；核心 77/77 通过。 |
| v2.3.0 | 2026-09-04 | **道具页搜索过滤（快速定位要删除/修改的道具）**：道具表格上方新增搜索行——`search_edit` 搜索框（支持 **中文 / 拼音全拼 / 拼音首字母 / 数字**，复用「新增道具」的 `filter_item_names`+拼音表）+ `search_mode_combo` 匹配方式（自动匹配/仅中文/仅拼音/首字母）+ ✕ 清除按钮；输入实时过滤（150ms 防抖 `_search_timer`，避免大档每敲一键整表重建卡顿）。过滤**不重排数据**：`_build_filter_map`(1526) 计算「表格可见行 → self._items 真实索引」映射 `self._filter_map`（set 判断保同名不遗漏），`_reload_table`(1557) 只渲染匹配行、`_fill_item_row`(1545) 的 # 列显示完整列表里的原序号，计数标签在过滤时显示「道具总数: N · 显示: M」；`_on_item_changed`/`_on_delete` 经 `_row_to_item_index`(1537) 做行号→真实索引映射，故**过滤下双击编辑、删除选中、表头排序均准确映射不串行**；批量按钮（全部改为/一键 _GetTimeNew→0）仍针对完整道具列表。新增/一键添加道具后调 `_clear_search`(1516) 自动清空过滤使新行在完整列表末尾可见。GUI 冒烟测试新增搜索段用例，94/94 通过；核心 77/77 通过。 |
| v2.2.1 | 2026-09-03 | **一键全部默认不再卡（补丁）**：原实现对每个字段都重新定位+整文本拼接替换(O(k·n)，10s+)。改为 `_apply_subs`(758) 一次性分块重建、`set_scalar_defaults_in`/`set_flat_dict_all` 走它；`_apply_char_defaults` 单遍替换后只原地刷新该页数值框（`_sync_char_spins`），不再整档重建；容器定位加文本对象缓存（`_char_open_cache`，text 替换即失效），打开后第一次点也免整档定位。真实 5.5MB 档实测：主角/队友 一键全部默认 10s+ → 约 0.3~0.5s（余量为一次容器扫描）。 |
| v2.2.0 | 2026-09-03 | **多线程 + 数据驱动建模，根治打开大档卡顿**：①核心扫描提速——`_matching_end`/逗号扫描改 **C 级正则**整体跳过引号串、`_num_token` 用 pos 免整段切片（实测角色页重建 33s→~3s）；②角色页改**数据驱动**：新增 `collect_page`(938，单遍收集某角色全部标量+字典条目)/`char_model`(966，整档角色模型)/`friend_blocks`(915，枚举带回开括号)，`_refresh_char_tabs`(1427)/`_apply_char_model`(1448) 与 页面构建器 都吃预收集数据，不再每个分组反复全量扫描；③**大档后台线程打开**：`_open_path`(1873) 对 ≥600KB 文本转 `_open_big`(1919)——子线程做 parse_itemhad+char_model+道具名收录（`done` 标志收尾防竞态），主线程泵事件、忙光标（`_set_busy` 1903 禁工具栏），完成后主线程建表/建页；打开/打开上次/最近/重新读取/粘贴统一走 `_open_path`，小档与测试仍同步。真实 5.5MB 档后台打开验证通过（10 队友页签、忙复位、括号平衡）。 |
| v2.1.0 | 2026-09-03 | **队友改为按 _AbName 自动枚举（覆盖在队+离队+未来新增）**：探查真实存档发现队友属性块不只在 `saveFriendData.value.AddFriends`(在队 6 名)，还有 `LeaveFriends`(离队好友 4 名：阿守/狼萤/虎儿精/鹿柒，结构相同)。新增纯函数 `friend_records(text)`(907)：遍历 `saveFriendData.value` 每个直接对象容器，把其中**直接含 `_AbName` 字符串字段**的子对象都当作一名可改队友返回 [(容器, 队友键, 中文名)]——今后游戏往任意容器(或新增容器)加新队友，读档后都会**自动**多出对应页签，无需改代码；`_refresh_char_tabs`(1367) 改用它枚举并逐个建页（scope=saveFriendData.value.<容器>.<键>），页签「队友·名字」，非 AddFriends 容器追加“(离队/容器名)”标注（`FRIEND_GROUP_LABELS` 904）；`_build_friend_page`(1676) 改签名(group,key,disp)。真实档端到端验证：10 名队友页签全部生成，离队阿守一键默认/武学经验可改且括号平衡。 |
| v2.0.0 | 2026-09-03 | **多角色分页（主角 + 每个队友 + 地图标注点）**：主窗口改 **QTabWidget 分页**（页0=道具，读档后动态加「主角」与每个「队友·名字」页签）。新增一套**精确子串替换引擎**（纯函数：`_entry_values`/`_locate_container`/`get_scalar`/`set_scalar`/`set_flat_dict_all`/`add_str_array`…，按 key 路径在 `savePlayerData.value`/`saveFriendData.value.AddFriends.*`/`saveCustomData.value` 内定位直接字段，只替换数字记号/编号字典数值/数组元素，键名·缩进·其余段落原样保留）。每页：①标量数值字段全部列出可直接改，带默认的字段有点“默认 N”按钮 + 顶部「一键全部按默认」——主角 `PLAYER_DEFAULTS`(Lv99/Exp1亿/AddProSpot999/UseProSpot0/AddLifeSpot999/UseLifeSpot0/力道…速度999/TalentAdd999/UseLifePeak0)、队友 `FRIEND_DEFAULTS`(_Lv99/_AddProSpot999/_UseProSpot0/六维999…)；②编号字典分组整组一键——主角页 KongFuTypeLv(武学经验 999999)/LifeExp(生活技能 999999)/SixProCurrent(六维 999)/FriendLoveNum(好感度 999，行首显示好友名)、每个队友页 SixProCurrent(999)/_KongFuTypeLv(999999)；③「地图标注点(_ActiveMap)」可输入新增/选中删除(存 saveCustomData.value)。角色页修改与 ItemHad 一起走「保存/另存为/复制结果」写回（改动即入 `self._text`，`_generate` 统一输出）。参考真实存档结构（savePlayerData.value / saveFriendData.value.AddFriends 6 队友 / saveCustomData.value._ActiveMap）。 |
| v1.9.0 | 2026-09-03 | **批量目标值持久保存（新增 设置.json）**：把“全部改为”的默认 9999 改成了**可改且自动记住**——右侧「全部改为:」数字框值变化后 500ms 防抖自动写盘（`_on_batch_value_changed`/`_autosave_settings`/`_save_settings_now`），退出 `closeEvent` 强制保存；左侧快捷按钮文字实时同步为“全部改为 <目标值>”，重开程序自动恢复上次的目标值。设置存程序目录 `设置.json`（`get_settings_path`/`load_app_settings`/`save_app_settings`，原子写盘、损坏回退默认 9999）。 |
| v1.8.1 | 2026-09-03 | **表头排序往返改为自记状态（修复“只降不升”类问题）**：`_on_header_clicked` 不再读取 QHeaderView 的 `sortIndicatorOrder()` 判断方向（不同平台/残留指示符可能误判），改由 `self._sort_col`/`self._sort_asc` 自己记住上次方向——**第一次点或换列=升序(小→大)，同列再点=降序(大→小)，可一直往返**；`_sort_items` 写入时同步记录状态，右键菜单选的方向也与表头点击联动。 |
| v1.8.0 | 2026-09-03 | **新增「一键整理道具名」**：列表工具行新增按钮 `btn_organize` → `_on_organize_names`——把当前打开的存档(表格)里已有的道具名（含手动新增/改名后的自定义名）与 道具名.json 比对，缺失的自动补入 json（复用新纯函数 `merge_item_names`，读档自动收录逻辑 `merge_item_names_from_text` 也统一委托给它）；有新增时弹窗列出新名、无新增仅状态栏提示；与“读档自动收录”互补，保证 json 始终跟上你已获取/编辑得到的道具名（不重名）。 |
| v1.7.0 | 2026-09-03 | **表头排序支持显式正/反向**：重构出统一排序入口 `_sort_items(col, reverse)`（可升序/降序、正反向）；保留“点击列头=升序、同列再点一次=反向(降序)、换列回到升序”的点击交互；新增**右键表头菜单** `_on_header_menu`（道具名称/数量 × 升序/降序 四项可直接选），表头加操作提示 tooltip。排序仍直接作用于 `self._items` 后整表重建，不影响编辑/删除映射。 |
| v1.6.0 | 2026-09-03 | **新增「仅显示缺少」对比补齐（在新增道具对话框内）**：勾选「仅显示缺少(未在个人背包)」（`only_missing_check`）即自动与 道具名.json 比对，只列出个人背包(当前 ItemHad)还没有的道具（`_missing_names`），底部实时统计 **道具名库 N / 个人已有 X / 缺少 M / 已选 K**（`_update_stat`）；可与搜索叠加过滤，配合「全选/多选」一次补齐全部缺少的道具；未勾选时仍显示全部、已有的灰显不可选。 |
| v1.5.0 | 2026-09-03 | **新增道具对话框大改造（搜索+多选+自定义）**：打开即可见 **道具名.json** 全部道具名（1419 个）列表；搜索框支持**中文 / 拼音全拼 / 拼音首字母 / 数字**（顶部可切 自动匹配/仅中文/仅拼音，`filter_item_names`，`_name_pinyin`）；可 **Ctrl/Shift 多选、全选（自动跳过已在列表的灰显项）** 一次添加多个（数量统一）；无匹配时输入即自定义名；双击或回车快捷加单项（`_on_double`/`_on_search_enter`/`chosen_names`）。拼音由程序目录 **拼音表.json** 提供（`load_pinyin_map`，脚手架 `_scaffold/build_pinyin_map.py` 用 pypinyin 从 道具名.json 生成 1014 汉字，**运行时不依赖第三方库**，缺失时自动退化为中文/数字搜索）；`_on_add` 改为批量添加 + 自动跳过已存在 + 非法名提示。 |
| v1.4.0 | 2026-09-03 | **新增表头排序（不影响修改）**：点击表格表头「道具名称」按名排序、「数量」按值排序，同列再点一次反向（`_on_header_clicked`，表头显示排序箭头 ↑/↓）。排序**直接对 `self._items` 排序后 `_reload_table` 整表重建**——表格行序恒等于列表序，因此排序后双击改名/改数量、删除选中仍按原行号映射到正确道具（不用 QTableWidget 内置排序，避免视觉行与数据行错位导致改错行）。 |
| v1.3.1 | 2026-09-03 | **读档比对入库加固（保证不重名）**：`merge_item_names_from_text` 每次读取存档（打开/粘贴/打开上次/最近历史/重新读取，均走 `_load_text`）都会把档内 ItemHad 道具名与 道具名.json 比对，只收录新名、已存在的自动跳过；若库文件存在**历史重复条目会自动清洗去重**（仅在有新增或需清洗时才写盘，正常无变化不写盘）；「一键添加全部道具」`_on_add_all_items` 加载时先 `dict.fromkeys` 去重，防库文件重复导致重复添加同名字典键。 |
| v1.3.0 | 2026-09-03 | **新增「打开上次 + 最近打开历史」**：工具栏新增「打开上次」（`_on_open_last` 一键重开上次文件）与「最近 ▾」下拉（`recent_menu`，`_rebuild_recent_menu` 展开时重建，点历史条目即直接打开）；历史存程序目录 `历史记录.json`（纯函数 `get_history_path`/`load_history`/`save_history`/`push_history`，最新在前、大小写去重、上限 10 条，菜单内可「清空最近记录」）；成功打开自动记入历史（`_record_history`），文件已不存在的条目自动移除（`_remove_history`）。**新增「重新读取」**：打开文件后若回到游戏又存档，点「重新读取」（`_on_reload`）再次读入当前文件内容刷新表格；新增脏标记 `_dirty`（未保存修改时窗口标题加 `*`，`_mark_dirty`/`_mark_clean`/`_refresh_title`），有未保存修改时重读先询问是否丢弃。打开逻辑统一收敛到 `_open_path(path, record=True)`。 |
| v1.2.0 | 2026-09-01 | **新增「一键 _GetTimeNew→0」**：把 ItemHad 全部道具的 `_GetTimeNew` 统一改为 0（`_on_set_get_time_zero`，仅改模型、点「保存」才写盘，带确认）；对已是 0 的道具自动跳过。 |
| v1.1.0 | 2026-09-01 | **新增道具名库**：①每次「打开/粘贴」读档自动把 ItemHad 段道具名**去重收录**到程序目录 `道具名.json`（仅取 ItemHad 段，避免混入装备/武功/好友/perk 等不可加入 ItemHad 的名字）；②新增**「一键添加全部道具」**——把道具名库中当前列表还没有的道具一次性加入（每个数量 9999），便于补全所有道具；③新增 `_scaffold\build_item_names.py` 批量收录脚本；由参考存档 `道具名称列表参考1.es3` 生成初始库 **757 个名字**（去重、排序）。 |
| v1.0.0 | 2026-09-01 | 首版。实现：打开/粘贴 es3、ItemHad 道具列表展示与直接编辑、一键全部改为 9999（可勾选跳过铜钱）、按任意值批量改、新增道具（数量默认 9999）、删除选中、保存/另存为/复制结果；核心为「行级解析 + 整段重建」，只动 ItemHad 段，其余段落原样保留；自动兼容 CRLF/LF 换行与 UTF-8 BOM；已有道具 `_DressType`/`_LockHp`/`_GetTimeNew` 均保留原值，新增道具分别记 1/0/0（参考存档含 7 处 `_LockHp=100`，必须保留勿清零）。 |

---

## 二、功能清单

- [x] **程序图标（v2.4.1）**：程序/主窗口/对话框使用 程序目录 `icon.ico`（`app_icon()`(259) 读取，缺失回退系统默认）；Nuitka 打包 `--windows-icon-from-ico=icon.ico` 为 exe 设图标并随 exe 带 `icon.ico`
- [x] **新增道具按分类浏览（v2.6.0）**：「新增道具」弹窗左栏新增分类树（可勾选）——按 内存所有道具.json 的 dataset 路径分级(大分类→小分类→细分类)，覆盖 equip装备/item道具/recipe配方/bookcontent书籍/kangfu武功秘籍/skillcom技能合成/shanhailu山海录 7 大类；勾选某分类=把该分类及全部下级道具纳入右侧候选，可多选大/小/细分类、勾「大分类」自动含其所有下级、展开反选子分类可精确排除(父自动半选)；右栏保留 搜索/多选/全选/自定义名/「仅显示缺少」，可分类+搜索叠加（**默认不勾选**：勾某分类右侧即只显示该类道具、空态有引导提示、未勾选但有搜索词则全库给建议、「全勾选」=全部）。数据源 `道具分类.json`(由 `_scaffold/build_item_categories.py` 生成)，缺失自动回退 道具名.json 平铺
- [x] **商店NPC 分类筛选（v2.5.1）**：「商店NPC」页签左侧新增分类下拉——按 地方/门派 过滤 NPC（安居城/禾兴城/广临城/黑市/临州城/龙居城/门派/其他/吾天城/乡火村/息宁城/岳汉城/第五章秘境…，取自 `内存所有道具.json` 的 `dataset/shop/<分类>/`, 由 `_scaffold/build_shop_cities.py` 生成 程序目录 `商店分类.json`）；可与 NPC 名字 中文/拼音/首字 搜索叠加；无法匹配的店归「(未分类)」
- [x] **NPC/商店卖的道具（v2.5.0）**：「商店NPC」页签左侧列出 `saveUtilData.value._ShopData` 的 NPC/商店（abName，支持 中文/拼音/首字 搜索），选中后右侧显示其 SellItems 卖品（名称/数量）；「添加卖品…」从 道具名.json 搜索（中文/拼音/首字/数字）/多选一次添加多个（数量默认 999，可改），与已有同名自动跳过；写入存档保存时随文件落盘
- [x] **商店参数 折扣/刷新日（v2.6.0）**：「商店NPC」页右侧「NPC 参数」行——折扣 Discount（1=无折扣，0~1 可调 0.05 步，一键默认 0.1）、刷新日 RefreshDay（一键默认 1）；选中商店即载入，改动即写入存档；该店无对应字段则禁用
- [x] **商店 SellEquips 添加装备（v2.6.0）**：「商店NPC」页「＋ 添加装备…(SellEquips)」——把 道具分类.json 的 **equip 装备类**（名含前导数字=等级，如 1铁盔→_Lv 1）加进该 NPC 的 SellEquips 装备栏：新增道具对话框多选 equip（件数=同名副本数，默认 1）；同名已有→向该名实例数组追加副本（可叠加），新名→新增条目；实例字段按游戏格式（_Name/_Lv/_Durable/_SkillLv/_ItemSkillLv/_DressType/_GetTimeNew/_IsCanLingHua/_IsFirstGet/_LockDissolve/_QiItemNames），缩进制表符/空格与 CRLF 自动兼容；标题显示 卖品 N 件 · 装备 K 种/M 件
- [x] **多线程全覆盖 + 表格局部刷新（v2.6.0）**：统一后台助手 `_run_bg`（大档打开/粘贴/保存/另存/复制 全程不卡, 忙光标+禁用工具栏+泵事件）；道具表格 批量改只刷数量列、删除走局部 removeRow+顺延 # 号（几千行不整表重建: 2.2s→约 0.13s/0.25s）；`_reload_table` 重建防闪烁；道具分类.json / 拼音表 读取内存缓存（新增道具对话框不再重复解析 json）
- [x] **打开文件**：QFileDialog 选择 `*.es3` 存档（支持任意 `*.es3`）
- [x] **粘贴内容**：把整份存档文本粘贴进来解析（无需落盘）
- [x] **道具列表**：表格枚举 ItemHad 全部道具的 名称 + 数量，可直接双击单元格修改
- [x] **道具搜索过滤（v2.3.0）**：道具页表格上方搜索框，支持 中文/拼音全拼/拼音首字母/数字 实时过滤（匹配方式可切 自动/仅中文/仅拼音，✕ 清除）；只显示匹配行便于快速定位要删除/修改的道具（# 列显示完整列表原序号，过滤下双击编辑/删除选中/排序映射准确，计数显示「总数·显示」）；清空即恢复全部；新增/一键添加后自动清过滤使新行可见
- [x] **表头排序（v1.4.0，v1.7.0 双向显式）**：点表头「道具名称 / 数量」按名称/数量排序——点击=升序、同列再点=降序反向（箭头指示）；右键表头可直接选「升序 / 降序」；排序不影响修改
- [x] **一键 9999**：全部道具数量改为 9999；「跳过『铜钱』」勾选（默认开）可保留铜钱原值
- [x] **按任意值批量改**：右侧数字框填目标值 + 「全部改为」
- [x] **批量目标值持久保存（v1.9.0）**：「全部改为」的目标值改后自动记住(存 设置.json，防抖+退出保存)；左侧按钮文字同步显示该值，重开程序仍保留（不再每次重设 9999）
- [x] **多角色分页（v2.0.0）**：主区顶部页签 = 道具 | 主角 | 每个队友·名字（读档动态生成）；每角色页把全部标量数值字段列出可直接改，带默认按钮的字段点一下即恢复默认，也可「一键全部按默认」
- [x] **队友按 _AbName 自动枚举（v2.1.0）**：不写死容器——扫描 saveFriendData.value 全部直接对象容器(AddFriends 在队 / LeaveFriends 离队 / 未来新容器)，凡直接含 `_AbName` 的子对象都自动生成「队友·名字」页签（离队/未知容器加标注）；游戏新增队友无需改代码
- [x] **主角/队友一键默认（v2.0.0）**：主角 Lv→99、Exp→1亿、AddProSpot→999、UseProSpot→0、AddLifeSpot→999、UseLifeSpot→0、力道/感知/灵气/体魄/经脉/速度/悟性/移动/特性点→999、已用生活点数→0；队友 _Lv→99、_AddProSpot→999、_UseProSpot→0、六维(_Power…_BerathSkill)等→999
- [x] **武学/生活/六维/好感 分组一键（v2.0.0）**：主角 KongFuTypeLv/LifeExp 一键 999999、SixProCurrent 一键 999、FriendLoveNum(显示好友名) 一键 999；每个队友页 SixProCurrent 999、_KongFuTypeLv 999999
- [x] **地图标注点（v2.0.0；v2.3.1 输入搜索+防重；v2.4.0 批量添加）**：显示 _ActiveMap 现有地名，可输入新增 / 选中删除（存 saveCustomData.value）；输入框输入时实时过滤下方已有地名便于查重（仅隐藏不匹配行、清空恢复全部），添加与已有相同的地名会被拦截并提示，不会重复添加；「从地图跳转点批量添加…」可搜索/多选/全选一次添加多个（默认只列未标注的，与已有重复自动跳过）
- [x] **地图跳转点库（v2.4.0）**：`_scaffold/build_map_jump_points.py` 从 MapJumpTable.bytes 提取全部跳转点显示名(name, 含无 id 记录)去重排序 → 程序目录 `地图跳转点.json`（顶层纯数组, 236 个, 真实档 _ActiveMap 29 全覆盖）；读取 `get_map_jump_path`/`load_map_jump_names`
- [x] **多角色写回同一管线（v2.0.0）**：角色页改动与 ItemHad 一起经「保存/另存为/复制结果」写回；只精确替换对应数值，键名/缩进/其它数据段原样保留
- [x] **大档后台线程打开（v2.2.0）**：超过约 600KB 的存档，解析/角色模型改在子线程完成（`_open_big`），期间忙光标、工具栏禁用、窗口不卡死；打开/打开上次/最近/重新读取均生效
- [x] **数据驱动建模 + 扫描提速（v2.2.0）**：`char_model`/`collect_page` 单遍收集角色页数据（原 33s 级建页降到亚秒级），`_matching_end`/逗号扫描改 C 级正则、`_num_token` 免整段切片
- [x] **新增道具（v1.5.0 增强，v1.6.0 缺少对比）**：打开即见 道具名.json 全部道具名列表，支持搜索（中文/拼音全拼/拼音首字母/数字）+ Ctrl/Shift 多选/全选一次添加多个（数量统一）；已在列表的名字灰显自动跳过；也可直接输入自定义名；双击/回车快捷加单项；勾选「仅显示缺少(未在个人背包)」自动比对并只列个人背包还没有的道具（实时统计 库/已有/缺少/已选），配合全选一次补齐；按存档规律插入段尾
- [x] **拼音搜索（v1.5.0）**：由程序目录 `拼音表.json`（脚手架 `_scaffold/build_pinyin_map.py` 生成，运行时零第三方依赖）支持 中文/全拼/首字母/数字 检索
- [x] **删除选中**：删除选中行（可多选，带确认，Delete 键快捷）
- [x] **一键添加全部道具（v1.1.0）**：把道具名库中尚未拥有的道具一次性加入（每个数量 9999）
- [x] **一键 _GetTimeNew→0（v1.2.0）**：把 ItemHad 全部道具的 `_GetTimeNew` 统一改为 0（带确认，保存才写盘）
- [x] **打开上次 / 最近打开（v1.3.0）**：工具栏「打开上次」一键重开上次文件；「最近 ▾」列出最近打开历史（最多 10 条，存 `历史记录.json`），点条目即直接打开，菜单内可「清空最近记录」；文件已不存在的条目自动移除
- [x] **重新读取（v1.3.0）**：游戏再次存档后，点「重新读取」以磁盘文件为准重读当前文件内容并刷新表格；有未保存修改先询问（标题带 `*` 表示未保存）
- [x] **道具名库（v1.1.0，v1.3.1 加固）**：每次读档（打开/粘贴/上次/最近/重新读取）自动把 ItemHad 道具名与 道具名.json 比对，收录新名、已存在跳过、历史重复自动清洗——保证**不重名**，随玩随收集
- [x] **一键整理道具名（v1.8.0）**：把当前存档(表格)里已有的道具名即时补入 道具名.json（缺失新增、已有跳过、不重名）；手动新增/改名自定义道具名后点它即可同步进 json
- [x] **保存 / 另存为 / 复制结果**：写回原文件 / 存新文件 / 复制完整修改后文本到剪贴板
- [x] **数据安全**：只重建 ItemHad 段 + 角色页精确子串替换；其余段落（装备/武功/地图等）逐字节保留；UTF-8 BOM 与 CRLF/LF 原样保持
- **自动测试**：`_scaffold\smoke_test.py`（核心逻辑 108/108，含真实/参考两存档无损往返 + 道具名库 + 最近打开历史 + 拼音搜索用例 + **商店 SellItems(v2.5.0)** + **商店参数 Discount/RefreshDay(v2.6.0)** + **商店装备 SellEquips 添加/追加/读回纯函数(v2.6.0)** + **道具分类库纯函数(v2.6.0: load_item_categories 缺文件回退/build_item_cat_nodes 树构建与跨分类去重)** + **读取缓存命中(分类库/拼音表, v2.6.0)**）+ `_scaffold\smoke_gui_test.py`（GUI 154/154，offscreen，含最近打开/打开上次/重新读取/脏标记/新增道具对话框/**道具搜索过滤(v2.3.0)**/**地图标注点搜索防重(v2.3.1)**/**地图跳转点批量添加(v2.4.0)**/**商店NPC(v2.5.0)**/**商店NPC分类筛选(v2.5.1)**/**新增道具分类树(v2.6.0)**/**商店装备 SellEquips GUI 用例(v2.6.0)**/**多线程全覆盖+表格局部刷新(v2.6.0: 批量改局部刷数量/删除#顺延/后台保存复制大文本粘贴)**用例）

---

## 三、代码地图（行号以 v2.2.0 为准；标注 v2.3.0/v2.4.0/v2.5.x 的为本版新增/移位方法当时实测；v2.6.0 起在 1100 行处插入了道具分类库函数约 +100 行, 其后行号整体下移, 本版新增/对话框相关已标 v2.6.0 实测行号）

文件为纯 `\n` 换行，行号以 VS Code 编辑器为准。

| 符号 | 行号 | 说明 |
|------|------|------|
| `__version__` | 41(v2.6.0) | 版本号（窗口标题/说明引用） |
| `app_icon`(v2.4.1) | 259 | 程序图标(程序目录/icon.ico, 缺失返回空 QIcon); `main`(2762)/`MainWindow.__init__` 设窗口图标 |
| `APP_NAME` | 28 | 应用名 |
| `Es3Item` | 43 | ItemHad 单件道具：name / count / get_time / dress_type / lock_hp |
| `find_itemhad_line` | 58 | 定位 `"ItemHad" :` 行（完整键名匹配，避免误中 ItemHadTemp） |
| `parse_itemhad` | 67 | 解析 ItemHad 段 → (items, indent, close_idx) |
| `build_itemhad_lines` | 164 | 按原缩进/段头/段尾/换行风格整段重建 |
| `apply_itemhad_text` | 195 | 把新物品列表写回完整文本（只替换 ItemHad 段） |
| `read_es3_text` | 217 | 读文件，剥离 UTF-8 BOM → (文本, BOM) |
| `write_es3_text` | 228 | 写文件，补回 BOM |
| `validate_item_name` | 239 | 道具名校验（非空、不含 `{ } " \` 换行） |
| `app_dir` | 250 | 程序目录(开发=脚本目录, 打包=exe 目录) |
| `get_item_names_path` | 257 | 道具名库文件路径(程序目录/道具名.json) |
| `extract_item_names` | 262 | 从 ItemHad 段提取道具名(去重) |
| `load_item_names` | 276 | 读取道具名库(JSON 数组) |
| `save_item_names` | 289 | 道具名去重排序后原子写回 JSON |
| `merge_item_names` | 300 | 把一份道具名列表并入库(去重/清洗历史重复), 返回(新增, 总数) |
| `merge_item_names_from_text` | 315 | 读档收录: 提取 ItemHad 名后委托 `merge_item_names` |
| `get_map_jump_path` / `load_map_jump_names`(v2.4.0) | 325 / 330 | 地图跳转点库路径/读取(程序目录/地图跳转点.json, JSON 字符串数组) |
| `get_pinyin_map_path` | 322 | 拼音表路径(程序目录/拼音表.json) |
| `load_pinyin_map`(v2.6.0 实测, 已缓存) | 395 | 读拼音表 {汉字:[拼音]}, 缺失返回 {}; 结果按 path 存 `_PINYIN_CACHE`(392), 重复调用不重读文件 |
| `_name_pinyin` | 348 | 名称 → (首字母串, 全拼串), 兼容 str/list 值 |
| `filter_item_names` | 369 | 过滤: 自动/仅中文/仅拼音, 中文子串/全拼/首字母/数字 |
| `HISTORY_MAX` | 402 | 最近打开历史上限(10 条) |
| `get_history_path` | 405 | 最近打开历史路径(程序目录/历史记录.json) |
| `load_history` | 410 | 读取历史(字符串数组, 最新在前) |
| `save_history` | 423 | 历史原子写盘(失败静默) |
| `push_history` | 435 | 把 path 置顶+去重+限长并写盘 |
| `get_settings_path` | 449 | 程序设置路径(程序目录/设置.json) |
| `load_app_settings` | 454 | 读设置(JSON 字典), 缺失/损坏返回 {} |
| `save_app_settings` | 465 | 设置原子写盘(失败静默) |
| `PLAYER_DEFAULTS` | 483 | 主角一键默认值表(Lv99/Exp1亿/AddProSpot999/UseProSpot0/六维999/TalentAdd999/UseLifePeak0…) |
| `FRIEND_DEFAULTS` | 517 | 队友一键默认值表(_Lv99/_AddProSpot999/_UseProSpot0/六维999…) |
| `SIX_LABELS` | 550 | 六维编号→中文(1力道…6速度) |
| `KONGFU_EXP_DEFAULT`/`LIFEEXP_DEFAULT`/`LOVE_DEFAULT`/`SIXPRO_DEFAULT` | 553~556 | 武学/生活技能经验 999999、好感度/六维 999 的组默认值 |
| `_matching_end` | 559 | 括号配对(字符串内括号忽略) |
| `_string_end` | 580 | 跳到引号串尾(处理转义) |
| `_key_value_offset` | 595 | 取条目首个未入引号冒号(键引号可含冒号) |
| `_key_display` | 620 | 键原串→展示名 |
| `_quoted_strings` | 632 | 提取区域内全部引号串内容 |
| `_entry_values` | 647 | 把对象/数组体按 0 层逗号切成条目 |
| `_locate_container` | 694 | 按 key 路径逐层下钻(每层只认直接子字段)到容器开括号 |
| `_num_token` | 720 | 值起点取数字记号(含小数/e) |
| `get_scalar` | 728 | 读 scope 内数字字段原样字符串 |
| `set_scalar` | 741 | 改 scope 内数字字段(只替换数字记号) |
| `iter_flat_dict` | 758 | 读编号/名字→数值字典条目 |
| `set_flat_dict_all` | 774 | 字典全部数值改值(键/格式保留) |
| `get_str_array` | 796 | 读字符串数组(如 _ActiveMap 地点名) |
| `add_str_array` | 808 | 数组加元素(重名跳过) |
| `remove_str_array` | 831 | 数组删元素 |
| `SCOPE_PLAYER`/`SCOPE_MAP`/`SCOPE_FRIENDS` | 853~855 | 主角/地图/队友容器的定位路径常量 |
| `get_str_field` | 858 | 读 scope 内字符串字段内容(去引号) |
| `SCOPE_SHOP`(v2.5.0) | 962 | NPC/商店容器路径 saveUtilData.value._ShopData |
| `shop_records`(v2.5.0) | 966 | 一次枚举全部商店 [(键, abName, SellItems开括号)] |
| `shop_sellitems`(v2.5.0) | 1001 | 读某店 SellItems [(名称, 数量)] |
| `shop_add_items`(v2.5.0) | 1023 | 追加卖品(与已有同名自动跳过/指定数量/整段重建) |
| `shop_field`(v2.6.0) | 1089 | 读某店直接数字字段(Discount/RefreshDay…) 原始文本; 无字段返回 None |
| `shop_set_field`(v2.6.0) | 1102 | 改写某店直接数字字段(只改已存在字段) → (新文本, 是否变更) |
| `_shop_sellequips_open`(v2.6.0) | 1127 | 定位某店 SellEquips 字段的 `{`(该店没有返回 None) |
| `shop_equip_names`(v2.6.0) | 1138 | 读某店 SellEquips 已上架装备 [(干净名, 件数)]; count_too 附带件数 |
| `_equip_fields_text`(v2.6.0) | 1159 | 单实例字段模板(l3/l4 缩进; _Name/_Lv(前导数字)/_Durable100/_SkillLv/_ItemSkillLv 空对象/_DressType1/_GetTimeNew0/_IsCanLingHua true/_IsFirstGet0/_LockDissolve false; 首含 _QiItemNames 空数组+空白行) |
| `_equip_objects_text`(v2.6.0) | 1178 | 某装备名数组的 n 个实例段拼接(实例间 `},{`) |
| `_equip_new_entry_text`(v2.6.0) | 1191 | 新装备条目文本(`"名":[{...}]`; 空 SellEquips 时 `{` 后内联首键) |
| `shop_add_equips`(v2.6.0) | 1196 | 添加装备(adds=[(名,件数)]): 同名已有→该名数组追加副本, 新名→新增条目, 空 SellEquips→新建; 缩进按 SellEquips 行前导(L0)+unit(制表符/空格自动), 兼容 CRLF; → (新文本, 新增名→件数) |
| `SHOP_CITY_ZH`(v2.5.1) | 1062 | 商店分类代码→中文(安居城/禾兴城/黑市/门派…) |
| `get_shop_cities_path` / `load_shop_cities`(v2.5.1) | 1071 / 1076 | 商店分类库路径/读取(商店分类.json, 返回 name→code/order/counts) |
| `ITEM_CAT_ZH`(v2.6.0) | 1109 | 道具分类 7 大分类代码→中文(equip装备/item道具/recipe配方/bookcontent书籍/kangfu武功秘籍/skillcom技能合成/shanhailu山海录) |
| `get_item_categories_path`(v2.6.0) | 1115 | 道具分类库路径(程序目录/道具分类.json) |
| `load_item_categories`(v2.6.0 实测, 已缓存) | 1317 | 读 道具分类.json → {version,zh,cats:[{code,groups:{相对路径:[名]}}]}; 缺失/损坏返回 None(新增道具回退 道具名.json); 结果按 path 存 `_CAT_CACHE`(1314), 重复调用不重读不重解析 |
| `build_item_cat_nodes`(v2.6.0) | 1164 | **纯函数**: cats → (roots 分类树节点[{code,zh,own,children,total}], all_names 跨分类/路径去重全量) |
| `get_str_field` | 858 | 读 scope 内字符串字段内容(去引号) |
| `_obj_direct_str` | 866 | 读某对象开括号处的直接字符串字段内容(供 friend_records 免再定位) |
| `list_scalar_fields` | 878 | 列出 scope 内全部数字字段 |
| `friend_keys` | 891 | 队友键列表(AddFriends 直接键; 兼容接口) |
| `FRIEND_GROUP_LABELS` | 904 | 队友分组显示名: AddFriends=在队 / LeaveFriends=离队 |
| `friend_records` | 907 | **按 _AbName 枚举全部队友属性块**: 遍历 saveFriendData.value 每个直接对象容器(AddFriends/LeaveFriends/未来新容器), 把直接含 _AbName 的子对象都列出 → [(容器, 队友键, 中文名)]; 游戏后续新增队友自动收录 |
| `friend_blocks` | 915 | 同 friend_records 但额外返回每名队友对象开括号下标, 供建页免重复扫描 |
| `collect_page` | 938 | **单遍收集**某角色容器页面数据: scalars 全部数字字段 + wanted 里编号/名字字典条目(替代每分组一次全容器扫描) |
| `PLAYER_DICT_FIELDS` / `FRIEND_DICT_FIELDS` | 961 / 963 | 主角/队友页要展示为字典分组的字段名 |
| `char_model` | 966 | **整档角色模型**(纯计算, 可在后台线程跑): 一次收集 player 标量/字典、各队友数据、地图名, 供建页与后台打开复用 |
| `AddItemDialog`(v2.6.0) | 1340 | 新增道具对话框: **左分类树(道具分类.json) + 右候选列表(搜索/多选/全选/自定义/缺少对比)**; 缺分类库自动回退 道具名.json 平铺 |
| ├ `__init__`(v2.6.0) | 1355 | 载 道具分类.json→roots/all_names; 无则回退 道具名.json; 双栏布局; 默认不勾选分类(右侧空并给引导; 未勾选+搜索词=全库建议) |
| ├ `_build_category_pane`(v2.6.0) | 1449 | 左栏分类树(QTreeWidget 勾选): 7 大分类根(显示 code·中文(总数), 展开到 0 级) + 按钮 展开全部/折叠/全勾选/取消勾选 |
| ├ `_cat_item` / `_cat_label`(v2.6.0) | 1490 / 1503 | 分类节点建 item(递归子级, 勾选框) / 节点文字(含子树计数) |
| ├ `_set_subtree_state`(v2.6.0) | 1509 | 勾选状态向下传播(勾=全选其下级, 取消=去掉其下级) |
| ├ `_refresh_cat_ancestors`(v2.6.0) | 1515 | 改动后向上刷新父级: 全勾=Checked/全不勾=Unchecked/混合=半选 |
| ├ `_on_cat_changed`(v2.6.0) | 1532 | 分类勾选变化 → 传播+父半选+置脏+刷列表 |
| ├ `_cat_set_all` / `_cat_clear`(v2.6.0) | 1544 / 1553 | 全勾选(全部道具) / 取消全部勾选(回到空, 有搜索词则全库建议) |
| ├ `_selected_names`(v2.6.0) | 1562 | DFS 收集勾选分类覆盖名: Checked=整棵子树/Unchecked=跳过/半选=自身直挂+子级按状态; 跨分类去重 |
| ├ `_current_scope`(v2.6.0) | 1594 | 右侧候选范围(分类勾选覆盖; 全不勾=空, 有搜索词则全库给建议) |
| ├ `_missing_names` | 1605 | 缺少名单: 全量库 - 个人已有(当前列表) |
| ├ `_filtered` / `_refresh_list` | 1609 / 1616 | 按当前分类 scope 搜索+仅缺少过滤 / 实时刷新列表 |
| ├ `_update_stat` | 1626 | 统计: 库总数/个人已有/缺少/已选 |
| ├ `_select_all` / `_select_none` | 1634 / 1641 | 全选(跳过已有) / 清空选择 |
| ├ `_on_double` / `_on_search_enter` | 1644 / 1649 | 双击/回车=加单项; 无匹配回车=自定义 |
| ├ `chosen_names` | 1661 | 本次名单: 单项优先→多选→搜索框自定义 |
| `MapJumpDialog`(v2.4.0, v2.6.0 实测) | 1670 | 从 地图跳转点.json 多选/全选添加地图标注点对话框: 搜索(中文/拼音)+匹配方式, 默认「仅显示未标注」, 已有项灰显; 双击/回车加单项 |
| `PasteDialog` | 1137 | 粘贴存档内容对话框 |
| `MainWindow` | 1159 | 主窗口(__init__ 1160: 排序状态 + 批量目标值设置 + 角色页状态 + _busy) |
| ├ `_mark_dirty` / `_mark_clean` | 1194 / 1200 | 未保存修改脏标记(标题加 `*`) |
| ├ `_refresh_title` | 1206 | 按脏标记刷新窗口标题 |
| ├ `_build_ui` | 1211 | 工具栏 + **QTabWidget 分页**(页0=道具, 其余由 `_refresh_char_tabs` 动态加) + 批量目标值行 |
| ├ `_sort_items` | 1354 | 统一排序入口: col(1名/2量)×reverse 升/降序, 记录状态后直接排 items 重建 |
| ├ `_on_header_clicked` | 1379 | 点列头: 第一次/换列=升序; 同列再点=反向(自记状态往返) |
| ├ `_on_header_menu` | 1389 | 右键表头菜单: 名称/数量 × 升/降序 |
| ├ `_on_search_changed`(v2.3.0) | 1511 | 道具搜索框/匹配方式变化 → 150ms 防抖后刷新表格 |
| ├ `_clear_search`(v2.3.0) | 1516 | 清空搜索并立即回到全量视图(新增/一键添加道具后自动调, 保证新行可见) |
| ├ `_build_filter_map`(v2.3.0) | 1526 | 按搜索文字算可见行映射 `_filter_map`(表格可见行→self._items 索引; 空=None); 复用 `filter_item_names`(中文/拼音/首字母/数字) |
| ├ `_row_to_item_index`(v2.3.0) | 1537 | 表格行号 → self._items 真实索引(考虑搜索过滤); 供 双击编辑/删除选中 映射 |
| ├ `_fill_item_row`(v2.3.0) | 1545 | 填充表格一行; # 列显示完整列表里的原序号(过滤下也准确) |
| ├ `_reload_table`(v2.3.0) | 1557 | 物品列表 → 表格(含搜索过滤: 过滤时只渲染匹配行, 计数「道具总数: N · 显示: M」) |
| ├ `_build_shop_tab`(v2.5.1, v2.6.0 实测) | 2370 | 构建「商店NPC」页签(固定 index=1): 左 分类下拉+NPC列表+搜索, 右 卖品表+「添加卖品…」+「＋ 添加装备…(SellEquips)」 |
| ├ `_refresh_shop_tab`(v2.5.1, v2.6.0 实测) | 2473 | 读档后重建商店缓存+填充分类下拉(商店分类.json)与 NPC 列表(每项存分类代码) |
| ├ `_filter_shop_list`(v2.5.1) | 1966 | NPC 过滤: 分类下拉(地方/门派) + 中文/拼音/首字搜索, setHidden 隐藏不匹配 |
| ├ `_show_shop_items`(v2.5.1, v2.6.0 实测) | 2557 | 显示当前 NPC 的 SellItems 卖品表(名称/数量); 标题含 卖品 N 件 · 装备 K 种/M 件 |
| ├ `_on_shop_add`(v2.5.1) | 2017 | 添加卖品: AddItemDialog(默认数量999)+shop_add_items 防重, 写 self._text 并 `_resync_shop_cache` |
| ├ `_on_shop_add_equip`(v2.6.0) | 2670 | 添加装备(SellEquips): 无字段提示; AddItemDialog 多选 equip(同名不灰显可追加副本, 默认数量1); shop_add_equips 写回 + `_resync_shop_cache` + 刷新 |
| ├ `_resync_shop_cache`(v2.6.0) | 2619 | 文本被改写(会移动后续偏移)后用新 shop_records 重算全部商店 SellItems 容器开括号缓存(防 添加卖品/装备 后旧偏移串位) |
| ├ `_refresh_char_tabs` | 1427 | **由 char_model 重建 主角 + 全部队友 页签**(数据驱动: 先一次性收集再建页, 大档不再逐分组扫几十次) |
| ├ `_apply_char_model` | 1448 | 用角色模型建主角/队友页签(纯控件构建) |
| ├ `_friend_display` | 1464 | 队友页签名: 优先 _Name 中文(兼容保留) |
| ├ `_mk_value_spin` | 1471 | 数值框(按原值定小数位/范围) |
| ├ `_char_page_scroll` | 1486 | 可滚动角色页容器 |
| ├ `_apply_scalar` | 1500 | 角色数字字段改值(写 self._text + 脏) |
| ├ `_apply_dict_all` | 1509 | 字典整组改值并同步 spin |
| ├ `_apply_char_defaults` | 1524 | 「一键全部按默认」(按默认表∩当前字段) |
| ├ `_add_scalar_section` | 1544 | 标量区(数据驱动 data_scalars): 每字段 数值框+默认按钮, 顶部一键全默认 |
| ├ `_add_dict_section` | 1590 | 编号/名字→数值字典区(数据驱动 data_entries + 组默认按钮) |
| ├ `_add_map_section` | 1636 | 地图标注点(_ActiveMap)列表+增删 |
| ├ `_map_filter`(v2.4.0) | 1998 | 输入地点名即过滤下方 _map_list(不匹配行 setHidden; 清空恢复全部), 供查重防重复 |
| ├ `_map_refresh` / `_map_add` / `_map_del`(v2.4.0) | 2010 / 2016 / 2043 | 刷新 / 添加 / 删除地点(刷新末尾重应用过滤; 添加同名地点被拦截并提示不重复) |
| ├ `_on_add_maps`(v2.4.0) | 2057 | 从 地图跳转点.json 批量/全选添加地图标注点(MapJumpDialog; 与 _ActiveMap 重复自动跳过, 成功后清空输入刷新) |
| ├ `_apply_dict_one` | 1696 | 字典单键改值(其余键不动) |
| ├ `_build_player_page` | 1721 | 主角页(数据驱动 model): 基础属性+武学+生活+六维+好感+地图 |
| ├ `_build_friend_page` | 1756 | 队友页(参数 group,key,disp,scalars,dicts): 属性+六维+武学经验(_KongFuTypeLv) |
| ├ `_on_item_changed` | 1787 | 表格单元格编辑校验/回写（名称查重、数量范围）+ 脏标记 |
| ├ `_load_text` | 1831 | 文本 → 解析 → 载入表格 → 自动收录道具名库 → **char_model 建页** → 清脏 |
| ├ `_lib_status` | 1855 | 状态消息附加道具名库收录情况 |
| ├ `_on_open` | 1864 | 打开文件对话框 → `_open_path` |
| ├ `_open_path` | 1873 | 统一打开入口: **大档(≥~600KB)转 `_open_big` 后台线程**, 小档同步; 记历史(文件不存在自动移除) |
| ├ `_set_busy` | 1903 | 忙状态: 等待光标 + 禁用工具栏 + 防重入(多线程期间) |
| ├ `_open_big`(v2.2.0; v2.6.0 实测 3395, 重构为调 `_run_bg`) | 1919 | **大档后台打开**: 子线程做 parse_itemhad+char_model+道具名收录, 主线程泵事件保持响应; done 标志收尾, 完成后主线程建表/建页/记历史 |
| ├ `_run_bg`(v2.6.0) | 3364 | 统一后台助手: 后台线程跑 work(纯计算/IO) + done + 忙光标 + 泵事件; 大档打开/粘贴/保存/另存/复制 全部复用(大档入口全程不卡) |
| ├ `_on_open_last` | 1973 | 打开上次文件(历史第一条) |
| ├ `_on_reload` | 1980 | 重新读取当前文件(有未保存修改先询问; 大档同样走后台线程) |
| ├ `_update_history_ui` | 1999 | 按历史刷新「打开上次」按钮可用性 |
| ├ `_record_history` | 2003 | 成功打开后记入历史(置顶/去重/限长) |
| ├ `_remove_history` / `_clear_history` | 2013 / 2020 | 移除某条 / 清空历史 |
| ├ `_rebuild_recent_menu` | 2026 | 「最近 ▾」展开时重建菜单(点条目即开/可清空) |
| ├ `_on_paste` | 2042 | 粘贴解析(成功后重建角色页); ≥600KB 走 `_paste_big` 后台 |
| ├ `_paste_big`(v2.6.0) | 3519 | 大段粘贴: 解析+角色模型+收录 后台线程, 完成回主线程建表/建页/清脏(不卡 UI) |
| ├ `_generate` | 2056 | 由当前物品列表 + self._text(含角色页改动) 生成完整文本 |
| ├ `_on_save` / `_on_save_as`(v2.6.0 实测 3555 / 3577) | 2062 / 2079 | 保存 / 另存为（成功清脏）; 重建+写盘后台 `_run_bg` |
| ├ `_on_copy`(v2.6.0) | 3604 | 复制结果: 后台重建大文本, 剪贴板写回主线程 |
| ├ `_reload_table`(v2.6.0) | 2363 | 物品列表→表格(过滤只渲染匹配行); 重建期间 setUpdatesEnabled(False)+blockSignals(几千行 ~-20%, 防逐行闪烁) |
| ├ `_sync_visible_counts` / `_set_count_label`(v2.6.0) | 2393 / 2386 | 批量改后只刷可见行数量列文本(局部不整表重建) / 计数标签 |
| ├ `_delete_rows_local`(v2.6.0) | 3744 | 无过滤删除走 removeRow 局部删 + 仅顺延 # 号(大表删 1 行 ~2.2s→~0.25s); 过滤下仍整表重建 |
| ├ `_on_copy` | 2101 | 复制结果到剪贴板 |
| ├ `_on_batch_value_changed` | 2114 | 批量目标值变化: 更新按钮文字 → 防抖自动保存 |
| ├ `_autosave_settings` / `_save_settings_now` | 2120 / 2123 | 500ms 防抖 / 立即写盘 设置.json |
| ├ `closeEvent` | 2127 | 退出: 停防抖定时器 + 强制保存设置 |
| ├ `_on_set_all_9999` | 2136 | 一键改(目标值=保存的批量目标值, 可跳过铜钱) |
| ├ `_on_set_all_value` | 2140 | 按输入值批量改 |
| ├ `_on_set_get_time_zero` | 2157 | 一键 _GetTimeNew→0 |
| ├ `_on_add` | 2174 | 新增道具(多选/自定义 → 批量添加+跳过已有+非法提示) |
| ├ `_on_delete` | 2210 | 删除选中 |
| ├ `_on_add_all_items` | 2225 | 一键添加道具名库中全部道具(数量 9999) |
| ├ `_on_organize_names` | 2247 | 一键整理道具名: 当前表格道具名并入 道具名.json(缺失新增/已有跳过) |
| └ `_on_help` | 2276 | 使用说明(v2.2.0 含大档后台读取说明) |
| `_HELP_TEXT` | 2280 | 说明文案(模块底部) |
| `main` | 2341 | 程序入口 |

---

## 四、构建与运行

### 4.1 开发运行
```powershell
# 在项目目录
python LingShouSaveEditor.py
# 或双击根目录 1.bat（cls + python LingShouSaveEditor.py）
```
依赖：`pip install PySide6`（Python 3.10 + PySide6，无需其它第三方库）。

### 4.2 Nuitka 打包
双击根目录 `0_nuitka_pyside6_clang_auto.bat`（或命令行执行），输出到 `build_output\`：
```bat
@echo off
chcp 65001 >nul
nuitka --standalone --enable-plugin=pyside6 --windows-console-mode=disable
      --follow-imports --jobs=4 --clang --remove-output
      --output-dir=build_output
      --include-data-files=地图跳转点.json=地图跳转点.json
      --include-data-files=道具名.json=道具名.json
      --include-data-files=道具分类.json=道具分类.json
      --include-data-files=拼音表.json=拼音表.json
      --include-data-files=商店分类.json=商店分类.json
      --include-data-files=icon.ico=icon.ico
      --windows-icon-from-ico=icon.ico
      LingShouSaveEditor.py
```
**唯一编译源**：`LingShouSaveEditor.py`。编译脚本只编译该文件；`地图跳转点.json` / `道具名.json` / `道具分类.json` / `拼音表.json` / `商店分类.json` 五个**静态库**经 `--include-data-files` 自动打进 exe 旁（v2.4.0 起；道具分类.json v2.6.0 加入），`历史记录.json` / `设置.json` 由程序运行时在 exe 旁自动生成、无需打包（`MapJumpTable.bytes` / `内存所有道具.json` 只是生成源, 不带进 exe）。⚠ **bat 编码注意**：bat 必须为 **UTF-8 无 BOM** 且首行 `chcp 65001 >nul`——cmd 默认按 GBK 解析 bat，含中文文件名的参数会被解析成乱码（如 `鍦板浘璺宠浆鐐?`）导致 Nuitka 报 “does not match any files”。

### 4.3 自动化测试
```powershell
& "D:/Program Files/Python310/python.exe" -u _scaffold/smoke_test.py       # 核心逻辑冒烟测试(含历史纯函数)
& "D:/Program Files/Python310/python.exe" -u _scaffold/smoke_gui_test.py   # GUI 冒烟测试(含最近打开/重新读取/多角色分页/地图增删/道具搜索过滤, offscreen)
```
`smoke_test.py` 会对**真实存档** `custom0.es3` 与**项目内参考存档** `道具名称列表参考1.es3` 做**只读**解析 + **无损往返**校验（原样重建必须与原文逐字节一致），并在内存里验证改 9999/新增/删除/清空、`_LockHp`/`_DressType` 字段保留、BOM/CRLF 兼容、道具名库提取(仅 ItemHad 段)/合并去重/落盘；另含**多角色纯函数**用例（`get_scalar`/`set_scalar`/`set_flat_dict_all`/地图数组增删/`friend_records` 在队+离队枚举等，v2.0.0 起）与 **商店 SellItems(v2.5.0)**、**道具分类库(v2.6.0: load_item_categories 缺文件回退 None / build_item_cat_nodes 树构建+子分类层级+跨分类同名字去重)** 用例；GUI 另含**道具搜索过滤**(v2.3.0)、**地图标注点搜索防重**(v2.3.1)、**地图跳转点批量添加**(v2.4.0)、**商店NPC(v2.5.0)**、**商店NPC分类筛选(v2.5.1)**、**新增道具分类树(v2.6.0)**用例。当前：核心 **98/98**、GUI **144/144** 通过。

道具名库批量收录(把任意 es3 的道具名并入 道具名.json):
```powershell
& "D:/Program Files/Python310/python.exe" -u _scaffold/build_item_names.py 道具名称列表参考1.es3
```

拼音表生成(道具名.json 收录到新汉字后重跑, 需要 pypinyin, 仅生成时需要):
```powershell
& "D:/Program Files/Python310/python.exe" -u _scaffold/build_pinyin_map.py
```

---

## 五、核心解析/生成逻辑（升级与扩展必读）

### 5.1 es3 存档里 ItemHad 的规律
```
		"ItemHad" : {"铜钱":{                    ← 段头 + 第一件物品头(内联)
				"_DressType" : 1,
				"_Count" : 998720404,            ← 数量
				"_Name" : "铜钱",                ← 与字典键同名
				"_LockHp" : 0,
				"_GetTimeNew" : 10101121200.116  ← 末字段无逗号
			},"2纯铜矿":{                        ← 上一件闭合 '}' + 下一件头 ',"名":{'
				"_DressType" : 1,
				"_Count" : 9999,
				"_Name" : "2纯铜矿",
				"_LockHp" : 0,
				"_GetTimeNew" : 0
			}
		},                                       ← 最后一件闭合 '}' + 段尾 '},'
```
- 每个道具固定 5 个字段，顺序为 `_DressType → _Count → _Name → _LockHp → _GetTimeNew`。
- 绝大多数道具 `_DressType=1 / _LockHp=0`，但**个别道具存在非默认值**（参考存档里 7 处 `_LockHp=100`、某些 `_DressType=2`）——本工具一律**保留原值**，仅新增道具用默认值（1 / 0 / 0）。
- 缩进：段头 2 个制表符、字段 4 个、物品闭合 `}` 3 个、段尾 `},` 2 个（**解析时按实际捕获，不硬编码**）。
- **每次游戏保存，ItemHad 的道具数量会增删变化**（这正是本工具要处理的核心）。

### 5.2 设计原则：只重建 ItemHad 段，不动其它
- es3 是 Unity EasySave 风格的文本序列化，**不是标准 JSON**，全文件通用解析成本高且易错。
- 本工具采取「**行级解析 + 整段重建**」：`parse_itemhad` 只解析 ItemHad 段 → 用户改物品列表 → `apply_itemhad_text` 把新段拼回 `lines[:start_idx] + 新段 + lines[close_idx+1:]`，**其它段落逐字节保留**。
- 因此改动永远只影响 ItemHad 段；段落之外的任何内容（包括空行、换行风格、缩进）原样保留。

### 5.3 兼容性细节（历史踩坑，勿回归）
1. **CRLF / LF 都要支持**：存档通常是 `\r\n`。凡以 `$` 结尾的行匹配（`^\s*\}$`、`^\s*\},$`）必须对 `ln.rstrip("\r\n")` 后的字符串匹配，否则 CRLF 下永远找不到段尾（曾导致解析扫到整个文件尾部、道具数虚高）。
2. **段头 `{` 的归属**：`find_itemhad_line` 的正则 `"ItemHad"\s*:\s*\{` 会把开头的 `{` 一起吞进匹配，故首件物品名应从 `suffix`（以 `"名":{` 开头）直接匹配 `"([^"{}]+)":\{`，不要再补前导 `\{`。
3. **勿误中 `ItemHadTemp`**：定位用完整键名 `"ItemHad"`（带引号），可避免命中 `"ItemHadTemp"` 等前缀相似段。
4. **BOM**：读写自动剥离/补回 UTF-8 BOM（`read_es3_text`/`write_es3_text`）。
5. **名称合法性**：道具名不能含 `{ } " \` 与换行（否则破坏格式）；新增/改名时用 `validate_item_name` 校验 + 查重。
6. **`_DressType` / `_LockHp` / `_GetTimeNew` 保留原值**：解析时以文本形式记录（`dress_type`/`lock_hp`/`get_time`，默认 None），重建时非 None 原样输出、None 才用默认值（1 / 0 / 0）。**历史教训**：曾把 `_LockHp` 硬编码为 0，导致参考存档往返不一致（该档有 7 处 `_LockHp=100`）；必须保留原值，否则会破坏存档。

### 5.4 扩展建议（后续功能入口）
- **新增设置项 / 历史**：最近打开历史（v1.3.0）存程序目录 `历史记录.json`（`load_history`/`save_history`/`push_history`），最新在前、大小写去重、上限 10 条，GUI 层在 `_record_history`/`_remove_history`/`_clear_history` 调用。若需持久化用户设置，建议沿用程序目录 JSON 式（与历史/道具名库同风格），键读写成对实现。
- **新增对 ItemHad 之外段的编辑**：以 `find_itemhad_line` 为模板，写对应段的定位/解析/重建函数（如 `find_xxx_line` + `parse_xxx` + `build_xxx`），再接入 GUI 新 Tab。

### 5.5 道具名库(道具名.json)逻辑
- **来源**：`extract_item_names` 只取 **ItemHad 段**（复用 `parse_itemhad` 的字典键），**不**扫全文件 `_Name`。
- **收录时机**：`_load_text`（打开/粘贴成功）后自动调 `merge_item_names_from_text` → 提取 + 与库合并去重 → 有新增才原子写盘（tmp+os.replace）。
- **存储**：程序目录 `道具名.json`（JSON 字符串数组，排序、去重、`ensure_ascii=False`）。打包 exe 后放 exe 目录（`app_dir()` 处理 frozen 分支）。
- **一键添加**：`_on_add_all_items` 读库 → 过滤当前已有 → 追加（数量 9999）→ 刷新表格。
- **注意**：早期曾全文件提取 `_Name` 得 1692 名，混入 perk(如 `铸甲匠1perk`)/好友名(兔千千/阿守)/技能效果名；改为**仅 ItemHad 段**后 757 名纯净无噪音。

### 5.6 拼音表(拼音表.json)逻辑
- **用途**：仅“新增道具”对话框搜索（中文/拼音全拼/拼音首字母/数字），运行时不依赖 pypinyin。
- **生成**：`_scaffold/build_pinyin_map.py` 用 pypinyin 从 `道具名.json` 提取汉字生成 `{汉字: [拼音]}`，当前覆盖 **1014 汉字**（约 14KB）。道具名库收录到**新汉字**后重跑即可。
- **读取**：`load_pinyin_map` 返回 dict（str 值自动包成 list），缺失/损坏返回 `{}` → 搜索退化为仅中文/数字（功能不中断）。
- **匹配**：`filter_item_names(names, query, pmap, mode)`，mode=0 自动 / 1 仅中文 / 2 仅拼音；`_name_pinyin` 数字/字母原样保留，未收录汉字退回其本身。

---

## 六、维护要点（每次修改后必须执行）

1. **文档+版本号同步（强制）**：改主程序后必须①同步更新本文件的 迭代记录/功能清单/代码地图 三处、②更新 `__version__`（第 41 行，窗口标题/说明均引用它）。
2. **版本号更新必须备份**：每次改版本号，把 `LingShouSaveEditor.py` 复制到 `version\` 子目录，命名 `LingShouSaveEditor - X.X.X 简短说明.py`。
3. **唯一编译源**：`LingShouSaveEditor.py` 是最终更新版，Nuitka 编译（`0_nuitka_pyside6_clang_auto.bat`）只使用该文件。
4. **脚手架入子目录**：测试/调试脚本必须放 `_scaffold\` 子目录（现有 `smoke_test.py` / `smoke_gui_test.py` / `build_item_names.py` / `build_pinyin_map.py`），不散落根目录。
5. **修改核心算法后必须跑两条冒烟测试**（见 4.3），重点回归：真实存档无损往返、CRLF、BOM、首件物品内联头、段尾闭合、道具名库提取/合并、最近打开历史读写/去重/上限、拼音搜索(中文/全拼/首字母)。
6. **道具名库（道具名.json）**：运行时会自动维护（读档即收录）；如需重建初始库：删掉 `道具名.json` 后运行 `_scaffold/build_item_names.py 道具名称列表参考1.es3`。它是**程序目录文件**（编译 exe 后放 exe 旁）。
7. **拼音表（拼音表.json）**：程序目录文件，新增道具搜索用；道具名库收录到新汉字后运行 `_scaffold/build_pinyin_map.py` 重新生成（仅需 pypinyin，运行时不依赖）。

---

## 七、目录结构

```
LingShouSaveEditor/
├── LingShouSaveEditor.py          # 主程序(唯一编译源, 最终更新版)
├── 0_nuitka_pyside6_clang_auto.bat # Nuitka 打包脚本
├── 1.bat                          # 开发运行脚本
├── 道具名称列表参考1.es3          # 参考存档(99级, 道具名来源参考)
├── 道具名.json                    # 道具名库(读档自动收录/去重, 程序目录文件)
├── 历史记录.json                  # 最近打开历史(最多 10 条, 程序目录文件, v1.3.0)
├── 拼音表.json                    # 汉字→拼音(新增道具搜索, 程序目录文件, v1.5.0)
├── 软件升级迭代记录.md             # 本文档
├── version\                       # 版本备份(每次改版本号放一份)
├── build_output\                  # Nuitka 输出(编译后生成)
└── _scaffold\
    ├── smoke_test.py              # 核心逻辑冒烟测试
    ├── smoke_gui_test.py          # GUI 冒烟测试(offscreen)
    ├── build_item_names.py        # 道具名库批量收录脚本
    └── build_pinyin_map.py        # 拼音表生成脚本(仅生成时需要 pypinyin)
```
